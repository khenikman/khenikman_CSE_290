// ISBNTester.cpp

#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include <sstream>


using namespace std;


void CodeEvaluator::readFile(const string&fileName){

    ifstream inputFile(fileName,ios::in);

    // before the while loop read the first line
    // assigning those prime numbers to 
    // subjectCategories

    


    string code ;
    while(inputFile >> line) {
        // Here you are creating the nodes
        // calling headInsert for each node



    }


}

void CodeEvaluator::headInsert(vector<int> data, string name) {



}

vector<int> parse(string &primeNumbers, char delimiter) { //is parse for determing category

    for (int i = 0; i < thirteen_digit_number.length() -1; i++) {
        if (thirteen_digit_number.substr(i, 1) == "-") {
                thirteen_digit_number.erase(i, 1);
                i--;
            }
        }
        all_isbn_inputs.push_back(thirteen_digit_number);
        linecounter++;
    

}




