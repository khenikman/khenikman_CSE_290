#include <vector>
using namespace std;
class Graph {

    public:

    vector<vector<int>> adjacencyList; //edge list
    int size;

// Constructor function to initialize the adjacency list


//function to add edges
void addEdges(int u, int v) {

    //adjacencyList[][]
    //look for in adjacency list the u element
    //check if u already has v for an edge
    // if it doesnt then add adjacencyList[i].push_back(v);

}

void dfs(int charNode) {

}
};

int main() {
    //initialize the adjacency list and assign size
    Graph g(10);

    //add edges

    //Breadth or depth first search
}